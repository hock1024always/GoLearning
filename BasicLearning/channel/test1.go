package main

// channel实现了goroutine实现两个进程之间的通信
//chan1 := make(chan int)     //make(chan Type)
//chan2 := make(chan int, 10) //make(chan Type, capacity)

//channel <- value 发送
//<- channel 接收并将其丢弃
//x := <- channel 接收并赋值
//x , ok := <- channel 接收并赋值，ok为false表示channel已关闭

func test() {
	
}
